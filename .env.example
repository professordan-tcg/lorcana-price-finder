# Copy to .env.local and set your key (do NOT commit .env.local)
JUSTTCG_API_KEY=tcg_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX


# Optional: set a GBP conversion rate (1 USD → ? GBP). Example: 0.78
# If omitted or invalid, prices remain in USD even if currency=GBP is requested.
JUSTTCG_GBP_PER_USD=0.78
```env
# Copy to .env.local and set your key (do NOT commit .env.local)
JUSTTCG_API_KEY=tcg_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
